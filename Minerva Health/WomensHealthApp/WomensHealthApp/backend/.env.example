# Copy to .env and fill in. On Railway, set these in the dashboard.

# From Railway Postgres: "Connect" → "Postgres URL" or DATABASE_URL variable
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

# Random string for signing JWTs (e.g. openssl rand -base64 32)
JWT_SECRET="your-secret-at-least-32-chars"

# Optional: port (Railway sets PORT automatically)
# PORT=3000
